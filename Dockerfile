# 1. Беремо готовий образ з Node.js
FROM node:20-alpine

# 2. Створюємо робочу папку всередині контейнера
WORKDIR /app

# 3. Копіюємо файли зі списками залежностей
COPY package*.json ./

# 4. Встановлюємо залежності всередині контейнера
RUN npm install

# 5. Копіюємо весь інший код проєкту
COPY . .

# 6. Відкриваємо порт, на якому працює React/Next (3000)
EXPOSE 5173

# 7. Запускаємо додаток
CMD ["npm", "run", "dev", "--", "--host"]